machine:
  node:
    version: 6.1.0
compile:
  override:
  - lein cljsbuild once server-prod
  - lein install
test:
  override:
  - node main.js
deployment:
  production:
    branch: dev
    commands:
      - rm -rf main.js build
      - npm install --save-dev deploy-to-git
      - npm run deploy

