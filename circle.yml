machine:
  node:
    version: 6.1.0
dependencies:
  pre:
    - npm install --save-dev deploy-to-git
compile:
  override:
  - lein cljsbuild once server-prod
test:
  override:
  - node main.js
deployment:
  production:
    branch: dev
    commands:
      - rm -rf build
      - npm run deploy

